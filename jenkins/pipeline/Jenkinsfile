pipeline {
    agent any
    stages {
        stage ('version') {
            steps {
                sh 'python --version'
            }
        }
        stage ('pipeline') {
            steps {
                sh 'python jenkins/pipeline/pipeline.py'
            }
        }
    }
}